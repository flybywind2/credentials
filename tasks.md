# Tasks

이 문서는 `prd.md`, `spec.md`, `trd.md`를 기준으로 작성한 구현 백로그입니다. 상태 표기는 `[x]` 완료, `[ ]` 미완료입니다. 우선순위는 `P0` 필수, `P1` 핵심 확장, `P2` 운영 고도화입니다.

## 현재 상태 요약

- [x] `spec.md`의 애플리케이션 UI/API 기능과 LDAP/SAML 인증 어댑터를 구현했다. 사내 IdP 접속 검증과 운영 부하/Docker 검증은 외부 인프라에서 확인해야 한다.
- [x] FastAPI 단일 포트 앱, DB 모델, 업무 CRUD, 판정, 승인, 조직/문항/툴팁/마감 관리의 핵심 흐름을 구현했다.
- [x] 최신 자동 검증을 완료했다: backend pytest 144건 통과, frontend node test 91건 통과.
- [x] Chrome DevTools 주요 화면 검증 스크린샷을 `docs/verification/`에 보관했다.
- [ ] 최종 운영 검증이 남아 있다: 191개 파트/5,000건 데이터 기준 부하 검증, Docker 실제 기동 검증.
- [ ] `P2` 운영 검증이 남아 있다: 성능 테스트 실행, Docker 실제 기동 검증, 운영 중앙 감사 로그 적재.

## SPEC 감사 결과

- [x] `P0` S01 로그인 화면과 사번 기반 mock SSO 인증 흐름을 구현한다.
- [x] `P0` 실제 사내 SSO 연동 코드를 구현했다: LDAP bind, SAML assertion 검증, bearer token 처리, 사용자/조직/역할 매핑.
- [ ] `P2` 사내 IdP/AD 실환경 접속 검증은 외부 인프라에서 수행해야 한다.
- [x] `P0` S06 승인 요청 확인 화면을 구현한다.
- [x] `P0` S03 스프레드시트 SPEC 정합성을 맞춘다: 파트 정보 표시, 고정 컬럼, 전역 저장 버튼, SPEC 기준 레이아웃.
- [x] `P1` S05 Excel Import도 미리보기/검증 화면을 거쳐 정상 행만 저장하게 한다.
- [x] `P1` S07 승인 대기 목록에 요청일시를 표시한다.
- [x] `P1` S08 반려 사유 모달 흐름과 항목별 검토 흐름을 SPEC에 맞게 정리한다.
- [x] `P1` S09 대시보드에 통합 비율 카드, 승인 현황 도넛 차트, 부서별 요약 필터/정렬을 추가한다.
- [x] `P1` S10 전체 조회의 실/팀/그룹/파트 필터를 계층형 드롭다운으로 변경한다.
- [x] `P1` S12 문항 관리를 탭 UI와 drag-and-drop 순서 변경으로 맞춘다.
- [x] `P1` S14 마감일 관리에 설명 입력 필드를 추가하고 저장한다.
- [x] `P0` 승인자의 데이터 읽기 권한을 “본인 산하 조직”으로 제한한다.

## 0. MVP 기반 작업

- [x] `P0` FastAPI 단일 서버에서 `/api/*`와 프론트엔드 정적 파일을 한 포트로 제공한다.
- [x] `P0` SQLite 기본 설정, SQLAlchemy `Base`, `SessionLocal`, `get_db()`를 구성한다.
- [x] `P0` 핵심 모델을 작성한다: 조직, 사용자, 업무, 문항, 승인 요청/단계.
- [x] `P0` 기본 API를 제공한다: 인증 사용자, 조직, 업무, 문항, 승인 대기, 대시보드.
- [x] `P0` vanilla JS 기본 UI를 제공한다: 입력자 표, 상세 모달, 승인자 목록, 관리자 요약.
- [x] `P0` pytest 기반 초기 테스트와 Chrome DevTools 프론트엔드 검증을 완료한다.

## 1. 데이터베이스 및 마이그레이션

- [x] `P0` TRD의 테이블 정의를 실제 SQLAlchemy 제약조건으로 보강한다.
- [x] `P0` SQLite 개발 DB 초기화 스크립트와 seed 데이터를 작성한다.
- [x] `P1` MySQL 호환성을 점검한다.
- [x] `P1` Alembic 마이그레이션 체계를 도입한다.
- [x] `P1` SSO/MySQL 연결 가이드를 `docs/sso-mysql-setup.md`에 저장한다.

## 2. 인증 및 권한

- [x] `P0` mock 인증을 사번 기반 로그인 API로 확장한다.
- [x] `P1` AD/LDAP/SAML SSO 어댑터 인터페이스를 만든다.
- [x] `P0` 실제 SSO 연동을 구현한다: LDAP bind 또는 SAML assertion 검증, 신뢰 가능한 bearer token 처리, 사용자 디렉터리 매핑.
- [x] `P0` 서버 사이드 권한 체크를 SPEC과 일치시킨다: 입력자 본인 파트 쓰기, 동일 그룹 읽기, 승인자 산하 조직 읽기, 관리자 전체 읽기/쓰기.
- [x] `P1` 프론트엔드 역할별 메뉴와 화면 접근 제어를 실제 사용자 권한과 연결한다.

## 3. 조직 관리

- [x] `P0` 조직 CRUD API를 구현한다.
- [x] `P0` CSV 조직 import를 구현한다.
- [x] `P1` 조직 관리 화면 `S11`을 구현한다: CSV 미리보기, 검색, 추가/수정/삭제, `{사번ID}@samsung.com` 이메일 자동 표시.

## 4. 문항, 예시, 마감일 관리

- [x] `P0` 기밀 문항/국가핵심기술 문항 API를 분리 구현한다.
- [x] `P0` 문항 추가 시 `"해당 없음"` 선택지를 자동 포함한다.
- [x] `P1` 관리자 화면에서 기밀/국가핵심기술 판단 항목을 조회, 추가, 삭제할 수 있게 구현한다.
- [x] `P1` 문항 순서 변경 기능을 구현한다.
- [x] `P1` 문항 순서 변경을 SPEC의 drag-and-drop 방식으로 변경한다.
- [x] `P1` 컬럼 예시/툴팁 관리 `S13`을 구현한다.
- [x] `P1` 입력 마감일 날짜 선택과 D-day 표시를 구현한다.
- [x] `P1` `S14` 설명 입력 필드를 구현한다.

## 5. 업무 데이터 입력

- [x] `P0` 업무 CRUD API를 실제 DB 기반으로 전환한다.
- [x] `P0` 입력 화면 `S03`의 행 추가, 행 수정, 저장, 상태 표시를 구현한다.
- [x] `P0` `S03`의 SPEC 레이아웃 정합성을 완료한다: 고정 컬럼, 파트 정보, 전역 저장 버튼.
- [x] `P0` 상세 입력 모달 `S04`를 업무 정보, 기밀, 국가핵심기술, Compliance, 데이터 보관 정보 구조로 구현한다.
- [x] `P1` `S04` 하단 버튼을 SPEC의 `저장`, `저장 후 닫기`, `취소` 구조로 맞춘다.
- [x] `P0` `"해당 없음"`과 다른 선택지의 상호 배타 체크박스 로직을 구현한다.
- [x] `P0` 기밀/국가핵심기술/Compliance 해당 시 데이터 유형과 소유자/사용자 필수 검증을 구현한다.
- [x] `P1` 반려 상태일 때 상단 반려 사유 배너와 항목별 검토 의견을 표시한다.
- [x] `P1` 입력자가 본인이 추가한 행만 삭제할 수 있게 구현한다.

## 6. 판정 및 검증 로직

- [x] `P0` 기밀/국가핵심기술 기본 판정 서비스를 작성한다.
- [x] `P0` 판정 로직을 업무 저장/수정 API에 연결한다.
- [x] `P0` `POST /api/tasks/validate`를 구현한다.
- [x] `P0` 승인 요청 전 전체 데이터 검증을 강제한다.
- [x] `P1` 프론트엔드 셀 단위 오류 표시와 오류 행 이동 기능을 구현한다.

## 7. 클립보드, Excel Import, Export

- [x] `P0` TSV 클립보드 붙여넣기 파서를 구현한다.
- [x] `P0` 붙여넣기 미리보기 `S05`를 구현한다.
- [x] `P1` Excel 파일 import를 구현한다.
- [x] `P1` Excel 파일 import를 `S05` 미리보기/검증 흐름으로 연결한다.
- [x] `P1` `GET /api/tasks/template` Excel 양식 다운로드를 구현한다.
- [x] `P1` `GET /api/export/excel` 전체/필터 Excel export를 구현한다.

## 8. 승인 프로세스

- [x] `P0` 조직 유형별 승인 경로 계산 서비스를 작성한다.
- [x] `P0` `POST /api/approvals/submit?org_id=`를 구현한다.
- [x] `P0` 승인 요청 전 `S06` 최종 확인 화면을 구현한다.
- [x] `P0` 승인 대기 목록 `GET /api/approvals/pending`을 실제 사용자 기준으로 구현한다.
- [x] `P1` 승인 대기 목록에 요청일시를 추가한다.
- [x] `P0` 승인 상세 검토 `S08`을 구현한다.
- [x] `P1` 승인자가 개별 항목에 대해 승인/반려 체크와 의견을 입력할 수 있게 구현한다.
- [x] `P1` `S08` 반려 버튼의 모달 사유 입력 흐름을 항목별 검토 UI와 정합화한다.
- [x] `P0` 승인 처리 `POST /api/approvals/{id}/approve`를 구현한다.
- [x] `P0` 반려 처리 `POST /api/approvals/{id}/reject`를 구현한다.
- [x] `P1` 승인 완료 후 수정 요청 플로우를 구현한다.

## 9. 관리자 대시보드 및 전체 조회

- [x] `P0` 대시보드 통계 API를 실제 DB 집계로 전환한다.
- [x] `P1` 관리자 대시보드 `S09`의 D-day, 입력 완료율, 주요 지표, 부서별 요약 테이블을 구현한다.
- [x] `P1` `S09` 통합 비율 카드, 승인 현황 도넛 차트, 부서별 요약 필터/정렬을 구현한다.
- [x] `P1` 전체 데이터 조회 `S10`을 구현한다.
- [x] `P1` `S10` 계층형 조직 드롭다운과 SPEC 기준 Excel 다운로드 배치를 구현한다.
- [x] `P1` 입력자 내 파트 현황 `S15`를 독립 화면 또는 명확한 전용 영역으로 구현한다.
- [x] `P1` 동일 그룹 내 조회 `S16`을 읽기 전용으로 구현한다.

## 10. 이메일 알림

- [x] `P1` SMTP/사내 메일 API 설정과 이메일 서비스 인터페이스를 구현한다.
- [x] `P1` 승인 요청 제출 시 다음 승인자에게 알림을 발송한다.
- [x] `P1` 단계별 승인 완료 시 다음 승인자와 관리자에게 알림을 발송한다.
- [x] `P1` 최종 승인 완료 시 입력자와 관리자에게 알림을 발송한다.
- [x] `P1` 반려 시 입력자에게 반려 사유 포함 알림을 발송한다.
- [x] `P2` 이메일 템플릿을 HTML로 정리하고 발송 실패 로깅을 추가한다.

## 11. 프론트엔드 품질

- [x] `P0` 1920x1080 기준 스프레드시트 레이아웃을 SPEC과 일치시킨다.
- [x] `P0` 모바일 전용 반응형 분기를 제거하고 데스크톱 최소 폭 기반으로 화면을 꽉 차게 조정한다.
- [x] `P1` 컬럼 헤더 툴팁 `(i)`를 예시 관리 데이터와 연결한다.
- [x] `P1` 상태 배지 색상 체계를 정리한다.
- [x] `P1` 키보드 접근성을 보강한다: 모달 focus trap, ESC 닫기, 버튼 aria-label.
- [x] `P1` Chrome DevTools 콘솔/네트워크/레이아웃 검증 스크린샷을 주요 화면별로 남긴다.

## 12. 테스트 및 검증

- [x] `P0` API 엔드포인트별 테스트를 실제 DB 기반으로 확장한다.
- [x] `P0` 판정 로직 테스트를 보강한다.
- [x] `P0` 승인 경로 및 승인/반려 상태 전이 테스트를 작성한다.
- [x] `P0` 권한 테스트를 작성한다.
- [x] `P1` CSV/Excel import/export 테스트를 작성한다.
- [x] `P1` 프론트엔드 smoke 검증을 자동화한다.
- [x] `P0` 2026-04-25 기준 backend pytest 144건, frontend node test 91건을 통과했다.
- [ ] `P2` 동시 접속 100명, 191개 파트, 5,000건 업무 기준 성능 테스트를 수행한다.

## 13. 배포 및 운영

- [x] `P0` Docker 실행 경로를 문서화한다.
- [x] `P1` 사내 프라이빗 클라우드 배포 설정을 분리한다.
- [x] `P1` 환경 변수 검증을 추가한다.
- [x] `P1` Python 3.10.19 런타임 기준을 고정하고 3.10 호환성 가드를 추가한다.
- [x] `P1` 운영 로그 포맷과 에러 핸들링을 정리한다.
- [x] `P2` 백업/복구, 감사 로그, 접근 로그 보관 정책을 `docs/operations.md`에 정의한다.
- [x] `P1` 일회성 취합 종료 처리를 구현한다: 관리자 잠금/재오픈, 마감 후 입력/수정/삭제/승인 요청 차단.
- [x] `P1` 최종 Export 기준을 고정한다: 파일명에 final/date를 포함하고 관리자 화면에 마지막 export 이력을 표시한다.
- [x] `P1` 최소 감사 로그를 남긴다: 로그인, 저장, 삭제, 승인 요청, 승인/반려, export.
- [x] `P1` 삭제/수정 추적을 감사 로그로 확인할 수 있게 한다.
- [x] `P1` 운영 시작 전 더미 데이터 제거 가이드를 문서화한다.
- [x] `P1` 메일 발송 실패 건을 관리자 화면에서 확인할 수 있게 한다.
- [ ] `P2` Docker 실제 기동 검증을 수행한다. 현재 실행 환경에는 `docker` CLI가 없어 검증하지 못했다.

## 14. 완료 기준

- [ ] 전 조직 191개 파트와 약 5,000건 세부업무 데이터를 입력/조회/승인할 수 있다.
- [x] 기밀, 국가핵심기술, Compliance 판정 및 필수 입력 검증이 서버와 UI에서 일관되게 동작한다.
- [x] 관리자, 입력자, 승인자 권한이 서버 사이드에서 SPEC 기준으로 강제된다.
- [x] 승인 요청, 승인, 반려, 재제출, 최종 승인 플로우가 파트 단위로 동작한다.
- [x] 관리자 대시보드가 SPEC의 모든 지표/차트/필터 요구사항을 충족한다.
- [ ] pytest, 브라우저 검증, Docker 실행 검증이 모두 통과한다.
  - [x] pytest 및 frontend node test 검증을 통과했다.
  - [x] 브라우저 검증 스크린샷을 보관했다.
  - [ ] Docker 실행 검증은 현재 실행 환경에 `docker` CLI가 없어 수행하지 못했다.

## 15. PRD/SPEC/TRD 재대조 추가 확인 항목

- [x] `P1` PRD의 “관리자와 승인자가 동일 인물인 경우 허용” 요구를 확인한다. `ADMIN`은 승인자 화면과 승인 API에 접근할 수 있다.
- [x] `P1` SPEC S08의 승인 상세 행 클릭 시 읽기 전용 상세 팝업을 명시 확인한다.
- [x] `P1` SPEC S10의 전체 조회 필터를 명시 확인한다: 실/팀/그룹/파트, 승인 상태, 기밀, 국가핵심기술, Compliance 필터.
- [x] `P1` TRD의 문항 개별 조회 API를 명시 확인한다: `GET /api/questions/confidential`, `GET /api/questions/national-tech`.
- [x] `P1` TRD의 대시보드 세부 API를 명시 확인한다: `GET /api/dashboard/completion-rate`, `GET /api/dashboard/approval-status`, `GET /api/dashboard/classification-ratio`.
- [x] `P1` PRD/SPEC의 S05 Excel/붙여넣기 미리보기 세부 UX를 추가 검증한다: 정상 행 선택 저장, 오류 수정 후 전체 저장, 오류 요약 표시.
- [x] `P1` TRD API 설계와 실제 라우터 경로의 호환성을 정리한다: `POST /api/tasks/bulk`, `GET/POST /api/admin/deadline`, export의 `approval_status` 파라미터 alias.
- [ ] `P2` 이메일 발송을 FastAPI `BackgroundTasks` 또는 비동기 큐로 분리하여 API 응답 지연을 방지한다.
- [ ] `P2` TRD DB 스키마와 실제 모델의 명명/상태값 편차를 정리한다: `deadline_settings` vs `system_settings`, `approval_steps.WAITING` vs `PENDING`, `task_question_checks.is_not_applicable`.
